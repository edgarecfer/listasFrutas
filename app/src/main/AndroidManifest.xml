<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.isc.listasdefrutas">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ListasDefrutas">
        <activity android:name=".UvaActivity"></activity>
        <activity android:name=".SandiaActivity" />
        <activity android:name=".NaranjaActivity" />
        <activity android:name=".ManzanaActivity" />
        <activity android:name=".MangoActivity" />
        <activity android:name=".LimonActivity" />
        <activity android:name=".KiwiActivity" />
        <activity android:name=".FresaActivity" />
        <activity android:name=".BananaActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>